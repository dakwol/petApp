import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    favouriteIcon: {
        height: 20,
        width: 20,
        resizeMode: 'contain'
    }
});

export default styles;
